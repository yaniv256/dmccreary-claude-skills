The generated file `23-strings-docker.md` creates a single MkDocs page with four Docker Python labs covering core string methods: Lab 1 demonstrates `upper()` and `lower()` for case conversion, Lab 2 shows `split()` breaking a sentence into a list of words (including a custom separator variant), Lab 3 shows `join()` as the inverse operation that glues a list back into a string with a chosen separator, and Lab 4 combines `strip()`, `split()`, and `join()` in a realistic "normalize messy input" pipeline. Each lab opens with a short conceptual explanation, provides 5–12 lines of readable starter code that produces immediate visible output, and closes with three targeted experiment suggestions that build from simple substitutions to more challenging modifications — following the "See It, Run It, Modify It" rhythm prescribed by the skill. All element IDs follow the required `docker-lab-N` / `docker-code-N` / `docker-run-N` / `docker-reset-N` / `docker-output-N` naming convention with unique suffixes 1–4, and the page opens with the standard instruction to start `scripts/run-python-docker.sh` before clicking Run.
