The generated file docs/intermediate/22-lists-docker.md teaches Python list operations through three interactive Docker labs that follow the same HTML/CSS/JS conventions already established by 21-docker-python.md. Lab 1 covers creating a list of planets, accessing items by positive and negative index, checking length, and slicing; Lab 2 demonstrates mutating a list with append(), remove(), pop(), and the `in` membership test using a shopping-list scenario; Lab 3 introduces list comprehensions with five progressively richer examples — squaring numbers, filtering evens, combining both, uppercasing strings, and filtering by word length. All three labs use the standard docker-lab-N element IDs (docker-lab-1/2/3, docker-code-1/2/3, docker-output-1/2/3) and call the shared runDocker()/resetDocker() functions already loaded globally via mkdocs.yml, so no inline CSS or JavaScript was needed in the page itself. Each lab includes concrete "Try these experiments" suggestions to encourage active exploration, and the page closes with a quick-reference table, a How It Works section, and a troubleshooting guide covering the most common list-related Python errors (ValueError, IndexError) as well as Docker connectivity issues.
